Place ffmpeg.exe and ffprobe.exe in this directory to bundle them with the application.

When these files are absent, the application searches Settings, its managed LocalAppData tools directory,
and Windows PATH. If no valid FFmpeg is found, it downloads and verifies the configured portable package.
Only use an FFmpeg build whose license terms you have reviewed and accepted.
